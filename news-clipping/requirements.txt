# 뉴스클리핑 — 필요 패키지
# 설치: pip install -r requirements.txt

# 네이버 API 통신
requests==2.31.0

# Gemini AI
google-generativeai==0.7.2

# 엑셀 파일 생성
openpyxl==3.1.2
pandas==2.1.4

# 시스템 트레이 아이콘
pystray==0.19.5
pillow==10.2.0

# 자동 발송 스케줄러
schedule==1.2.1

# 유사도 기반 중복 제거 (선택 — 없으면 자체 알고리즘 사용)
scikit-learn==1.3.2
